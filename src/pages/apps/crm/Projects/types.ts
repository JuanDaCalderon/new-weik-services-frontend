export type DailyTask = {
  title: string;
  shortDesc: string;
  time: string;
  teamSize: number;
};

export type TeamMember = {
  avatar: string;
  name: string;
  designation: string;
  experience: string;
};
